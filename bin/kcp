#!/bin/bash
for soft in $*; do
	pod=$(kubectl get pod | grep bastion | cut -d ' ' -f 1)

	echo "make build pkg=$soft static=true 2> /dev/null;"
	make build pkg=$soft static=true 2> /dev/null;

	echo "kubectl cp bin/$soft $pod:.";
	kubectl cp bin/$soft $pod:.;
done;
